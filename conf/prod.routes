# Add all the application routes to the app.routes file

->          /fset-fast-stream                   app.Routes
->          /fset-fast-stream/hmrc-frontend     hmrcfrontend.Routes
->          /                                   health.Routes
